# Rendering/ — Phase 2

Deliberately empty. The SpriteKit board, card nodes, map graph and animations
land here in Phase 2. Nothing in `GameCore/` may ever import SpriteKit or UIKit
(`GameCorePurityTests` enforces this).
